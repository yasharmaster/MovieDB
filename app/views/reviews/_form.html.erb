<%= simple_form_for([@movie, @movie.reviews.build]) do |f| %>

    <div id="rating-form">
        <%= f.input :rating %>
    </div>
    <%= f.input :comment %>
    <%= f.button :submit, :class => "btn btn-primary" %>
<% end %>